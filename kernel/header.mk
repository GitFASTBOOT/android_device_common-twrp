ifeq ($(BOARD_BOOTIMG_HEADER_VERSION),2)
BOARD_KERNEL_PAGESIZE := 2048
BOARD_FLASH_BLOCK_SIZE := 131072
endif

ifneq ($(filter $(BOARD_BOOTIMG_HEADER_VERSION),2 3),)
BOARD_PAGE_SIZE := 4096
BOARD_FLASH_BLOCK_SIZE := 262144
endif
